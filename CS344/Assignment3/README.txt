## HOW TO COMPILE SMALLSH ##

Option 1.

After extracting the zipfile and while in the smallsh directory, run

make

Option 2.

In the smallsh directory, on the command line, run

gcc -std=gnu99 -o smallsh smallsh.c
